# 隐私与安全

Voice Type 当前是开发预览软件，不适合未经评估就用于机密语音或敏感输入场景。

## 音频与文字

- “测试麦克风”只在本地计算音量，不连接云端；10 秒后自动停止。
- 用户开始录音时连接所选区域的阿里云 DashScope 服务并上传音频；识别需要网络，可能计费。
- 新版不把录音或转写持久保存为文件。结果仍会留在窗口内，隐藏到托盘不等于清除。
- 启用自动粘贴时识别结果会进入系统剪贴板，旧剪贴板不恢复。剪贴板管理器、系统同步及目标应用可能另行存储文字。
- 不会自动按 Enter，但目标应用如何处理粘贴内容不由本工具控制；请勿在终端命令行或敏感输入框盲测。
- 云端保留、处理区域及数据政策以服务商说明和你的账户协议为准，本项目不能承诺云端零留存。

## API Key

- 设置 JSON 不含 API Key；默认仅在本次进程内使用。
- 可选择保存到系统凭据库，条目标识为服务 `VoiceType`、账户 `dashscope`。凭据库不可用时退回进程内保存。
- 也可通过 `DASHSCOPE_API_KEY` 环境变量提供。优先级是本次填写的 Key、环境变量、系统凭据库。
- **取消勾选“保存”不会删除以前已保存的 Key。** 删除时请使用系统凭据管理工具，并清除自行设置的环境变量。
- 卸载 Python 包或删除桌面程序不会自动删除用户配置或系统凭据。Key 泄露后应在服务商控制台撤销并换新。

## 权限与报告问题

全局录音键需要监听输入，自动粘贴需要模拟粘贴键；macOS 等系统可能要求明确授权。
项目不提供绕过安全输入、系统保留键或受保护窗口限制的能力。

提交公开 Issue 前，检查日志、截图和配置：不要上传 API Key、认证请求头、私人转写或录音。
涉及漏洞或凭据泄露时，请通过代码托管平台提供的私密安全报告渠道联系维护者（若已启用）；
未有私密渠道时，可先公开请求联系方式，不要公开漏洞利用细节或密钥。

自有代码许可见 [LICENSE](LICENSE)，第三方分发义务见 [第三方许可](docs/third-party.md)。
